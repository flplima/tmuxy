/**
 * Jest Setup File for Tauri E2E Tests
 *
 * Loaded after the test framework is installed in the environment.
 * Sets up shared state and custom matchers.
 */

// Increase default timeout for all tests
jest.setTimeout(300000); // 5 minutes
