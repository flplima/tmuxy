# Logs
logs
*.log
npm-debug.log*

node_modules
dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Rust
target/

# Environment
.env

# Claude Code (user-specific, not checked in)
CLAUDE.local.md
.claude/*
# Track agent definitions, lib, and startup script
!.claude/agents/
!.claude/lib/
!.claude/start.sh
!.claude/README.md

# BDG session files
.bdg/

# Jest snapshots (auto-generated)
__snapshots__/

# Next.js
.next/
out/

# Storybook
storybook-static/

# v86 spike assets (large guest kernel + BIOS, fetched via `npm run fetch:v86-image`)
packages/tmuxy-ui/v86-assets/

# Core dumps (e.g., zsh crashes during dev)
core
core.*

# Generated wasm bindings (build via npm run build:wasm)
packages/tmuxy-wasm/pkg/
target/

# Local reference images (not tracked)
split-smooth-animations.png
