# Generated by a Rust test from the live wire types and compared byte for byte
# (packages/protocol-fixtures/README.md). Formatting them here would rewrite
# the bytes `cargo test` asserts on and turn the contract test red.
packages/protocol-fixtures/*.json
