---
name: release
description: Ship a tmuxy release — the full sequence from a green main commit to a Homebrew-installable build (version bump across 6 files, tag, tag-triggered CI, cask/formula bumps on the tap). Use when the user asks to cut, ship, tag, publish, or release a version, bump the version, or asks why brew is still serving the old build.
---

# Release Workflow

Each step must succeed before the next. **Never tag ahead of CI.**

The mechanical steps are scripted under `scripts/` in this skill directory; run them from the repo root. Each one refuses to proceed when its preconditions aren't met, so prefer them over doing the steps by hand.

| Step | Script |
|---|---|
| 2. Wait for CI green | `.claude/skills/release/scripts/wait-ci commit [<sha>]` |
| 3. Bump the version | `.claude/skills/release/scripts/bump-version [<version>]` |
| 4. Tag and push | `.claude/skills/release/scripts/tag-and-push <version>` |
| 5. Wait for the tag run | `.claude/skills/release/scripts/wait-ci tag <version>` |
| 6. Verify brew is ready | `.claude/skills/release/scripts/verify-release <version>` |

## 0. macOS signing (one-time setup)

The macOS build signs and notarizes itself when — and only when — these repository secrets exist. The Tauri CLI reads them during `tauri build`: it imports the certificate into a throwaway keychain, signs with the hardened runtime, then notarizes and staples. With `APPLE_CERTIFICATE` absent it skips signing silently, so forks and PRs still produce a working (unsigned) DMG.

| Secret | What it is |
|---|---|
| `APPLE_CERTIFICATE` | base64 of the **Developer ID Application** `.p12`. An *Apple Development* cert is NOT enough — it fails Gatekeeper on every machine but the ones registered to it |
| `APPLE_CERTIFICATE_PASSWORD` | the password set when exporting that `.p12` |
| `APPLE_SIGNING_IDENTITY` | the identity's full name, e.g. `Developer ID Application: Felipe Lima (NF27TT2TR8)` |
| `APPLE_API_KEY` | App Store Connect key ID (the `XXXXXXXXXX` in `AuthKey_XXXXXXXXXX.p8`) |
| `APPLE_API_ISSUER` | the issuer UUID shown above the key list |
| `APPLE_API_KEY_P8` | base64 of the `.p8` itself — staged to a file at build time, since notarization wants a path |

The API key is used rather than `APPLE_ID` + an app-specific password because it carries no 2FA and can be revoked without touching the Apple ID. Apple issues a `.p8` **once**; losing it means minting a new key.

Signing changes nothing about the cask — Homebrew already strips the quarantine xattr — but it removes the *"couldn't verify tmuxy's signer"* warning and lets the app open on a Mac that downloaded the DMG directly.

## 1. Land the change on main

Stage files explicitly, commit with a gitmoji prefix, push to `origin/main`. CI runs three workflows: `lint and tests`, `Build App`, `Deploy Demo`. The `Build App` workflow on a non-tag push builds and uploads artifacts but **skips** the `release` and `bump-cask` jobs — those are tag-gated.

## 2. Wait for CI green on the change commit

```
.claude/skills/release/scripts/wait-ci commit
```

Polls until all three workflows reach a terminal state; exits non-zero on the first failure. If `lint and tests` fails on something pre-existing (e.g. `cargo fmt --check` drift in a file you didn't touch), fix it as a separate commit per the "Testing & Bug Fixes" rule in the root `CLAUDE.md`, then wait again.

## 3. Bump the version

```
.claude/skills/release/scripts/bump-version            # alpha number + 1
.claude/skills/release/scripts/bump-version --dry-run  # preview
```

The next version is the existing version with the alpha number incremented. Six files must stay consistent, and the script refuses to run if they already disagree:

- `Cargo.toml` (workspace.package.version)
- `package.json` (root)
- `packages/tmuxy-ui/package.json`
- `packages/tmuxy-demo/package.json`
- `packages/tmuxy-tauri-app/tauri.conf.json` — a literal, because Tauri 2 forbids templating. `packages/tmuxy-tauri-app/build.rs` auto-syncs it from `Cargo.toml` at build time, but the synced value is committed explicitly so the tag is reproducible without a build step
- `Cargo.lock` — regenerated by `cargo build -p tmuxy-server`

It commits as `🚀 v<new-version>` with no body, stops `tmuxy-dev` in pm2 first (cargo racing the dev-mode binary yields transient `Permission denied`), and prints the file list to compare against `git show --stat v<previous-version>`.

## 4. Tag and push

```
.claude/skills/release/scripts/tag-and-push <new-version>
```

Verifies you're on a clean `main` and that `Cargo.toml` matches the tag, then runs `git tag` → `git push origin main` → `git push origin <tag>`. **Order matters:** main goes first so the tag's commit is on the remote when the tag arrives.

## 5. Wait for the tag-triggered Build App run

```
.claude/skills/release/scripts/wait-ci tag <new-version>
```

Pushing the tag triggers a **second** `Build App` run (this one with `github.ref = refs/tags/v...`). It executes `build` → `release` → `bump-cask` → `bump-formula`:

- **`release`** downloads the build artifacts and creates a GitHub Release with both Linux architectures — `tmuxy_<version>_amd64.AppImage`, `tmuxy_<version>_aarch64.AppImage`, `tmuxy_<version>_amd64.deb`, `tmuxy_<version>_arm64.deb` — plus `tmuxy_<version>_universal.dmg`. The `build` matrix produces the two Linux arches on the `ubuntu-22.04` (amd64) and `ubuntu-22.04-arm` (arm64) runners.
- **`bump-cask`** downloads the `.dmg`, computes sha256, and pushes an updated `Casks/tmuxy.rb` to `flplima/homebrew-tap` (macOS install).
- **`bump-formula`** downloads **both** Linux AppImages, computes a sha256 for each, and pushes an updated `Formula/tmuxy.rb` (with `on_arm`/`on_intel` url+sha256 blocks) to the same tap (Linux install). It runs **after** `bump-cask` — sequential, not parallel — so the two jobs don't race pushing to the same repo.

**This run is what makes `brew install --cask flplima/tap/tmuxy` (macOS) and `brew install flplima/tap/tmuxy` (Linux) pick up the new version.** Until it finishes green, brew still points at the previous tag.

## 6. Verify brew is ready

```
.claude/skills/release/scripts/verify-release <new-version>
```

Asserts the 5 release assets exist and that both `Casks/tmuxy.rb` and `Formula/tmuxy.rb` on the tap pin the new version. Exit 0 means a user running `brew update && brew upgrade --cask flplima/tap/tmuxy` (macOS) or `brew update && brew upgrade flplima/tap/tmuxy` (Linux) now gets the new build.

## Common failures

- **Linux build hangs on `npm install`** — runner-side flake. Cancel the hung run (`gh run cancel <id>`) and re-run the workflow (`gh run rerun <id>`). Deleting and repushing the tag also works but needs `--force` on the second push, so prefer the rerun.
- **`cargo fmt --check` fails on a pre-existing file** — run `cargo fmt -p <crate>`, commit as `🎨 cargo fmt <path>` before the version bump, and re-wait for CI.
- **Tag-triggered run starts but the `release` job is skipped** — the tag wasn't pushed (only the commit was). Confirm with `git ls-remote --tags origin`.
- **`bump-cask`/`bump-formula` fails with "already at v..."** — benign; a previous run already pushed that tap update. Brew is ready.
